/**
 * Returns a lowercase version of that string.
 * @param string - The string to format.
 * @returns the string in lowercase.
 */
const formatString = (string) => {
  return string.toLowerCase();
};

export { formatString };
